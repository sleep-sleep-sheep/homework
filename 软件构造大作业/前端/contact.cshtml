
   
    <style>
       

        /* 流星动画 */
        .meteor {
            position: absolute;
            width: 2px;
            height: 100px;
            background: linear-gradient(to bottom, rgba(255, 255, 255, 0), rgba(255, 255, 255, 1));
            animation: meteor 5s linear infinite;
            opacity: 0;
        }


        section {
            margin: 100px 0;
            perspective: 1000px;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            transform-style: preserve-3d;
            background-color: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
        }

        .card:hover {
            transform: rotateY(15deg) rotateX(-10deg) scale(1.08);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.25);
        }

        .card-body {
            padding: 30px;
        }

        .card-title {
            color: #764ba2;
            font-size: 2rem;
            font-weight: 600;
            display: flex;
            align-items: center;
        }

        .card-title i {
            margin-right: 10px;
            font-size: 1.5rem;
        }

        .card-text {
            color: #666;
            font-size: 1.1rem;
        }

        footer {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.8), rgba(118, 75, 162, 0.8));
            color: white;
            text-align: center;
            padding: 80px 0 30px 0;
            clip-path: polygon(0 15%, 100% 0, 100% 100%, 0 100%);
            box-shadow: 0 -5px 15px rgba(0, 0, 0, 0.2);
            position: relative;
            overflow: hidden;
        }

        footer p {
            margin-bottom: 0;
            position: relative;
            z-index: 1;
        }
    </style>

   

    <!-- 关于我们 -->
    <section id="about" class="container">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><i class="fa-solid fa-users"></i>关于我们</h2>
                <p class="card-text">我选择的是智慧旅行网页，通过前后端分离的方式来实现</p>
            </div>
        </div>
    </section>

    <!-- 服务 -->
    <section id="services" class="container">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><i class="fa-solid fa-briefcase"></i>服务</h2>
                <p class="card-text">关于技术实现方面，主要运用的了前端（html,css,javascript,vue框架，bootstrap框架，element-plus.ui框架）来实现页面的总体布局。
                    另外，还通过了丰富的动态布局，滑动布局等等。
                    在后端方面，通过mysqlconnector连接数据库mysql,使用deepseek-api来调用deepseek大模型，使用了响应式页面布局等等
                </p>
            </div>
        </div>
    </section>

    <!-- 联系我们 -->
    <section id="contact" class="container">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><i class="fa-solid fa-envelope"></i>联系我们</h2>
                <p class="card-text">如果您有任何问题、建议或者合作意向，都可以联系我们哦！</p>
            <p class="card-text">电子邮件：<a href="mailto:cld20051010@qq.com">cld20051010@qq.com</a></p>
                <p class="card-text">电话：123 - 456 - 7890</p>
            </div>
        </div>
    </section>

   

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>

    